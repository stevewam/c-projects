#include <iostream>

    #include <vector>

    #include <numeric>

    using namespace std;

int main(){
      int a = 2, c = 2;
      char ch = 'b';
      cout << -a * c++;
      cout << (ch += 2);
      std::find_if()
    }